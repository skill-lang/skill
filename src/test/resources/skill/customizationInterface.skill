#ensure that customizations are pushed down on project just like regular fields

interface CustomFields {
  custom ada
  !with "RFG.Node"
  "RFG.Node" node;

  custom java
  !import "my.import"
  !modifier "public synchronized"
  "Object" any;
}

Projected : CustomFields {}


typedef long v64;
